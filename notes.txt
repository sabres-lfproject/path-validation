after change to the local repo:

sudo docker build -t sabres_node:latest .

#sudo docker tag 30d557e689d5 sabres_node:latest
#docker run sabres_node:latest

sudo docker-compose up -d --scale sabres_node=4

sudo docker exec -ti sabres_sabres_node_1 /bin/bash


./xorHash "../data/1.secret" "../data/new.secret" # in the sabres/data repo




# Test even number of nodes, all nodes starts with two secrets being 1 (64 bit), after xor, Node 1 should have a new secret of 0 


curl --location --request POST '0.0.0.0:49187/backward' \--header 'Content-Type: application/json' \--data-raw '{"filename": "new.secret"}'


shell.exec('./xorhash.sh ' + '1.secret' + ' ' + 'new.secret');

curl --location --request POST '0.0.0.0:49194/sendContent' \--header 'Content-Type: application/json' \--data-raw '{"filename": "new.secret"}'

vim data/new.secret 
